import {Task} from "../Task";
import {ISortingStrategy} from "./ISortingStrategy";

// export class PrioritySortingStrategy implements ISortingStrategy {
//     sort(tasks: Task[]): Task[] {
//         return tasks.slice().sort((a:, b) => a.priority - b.priority);
//     }
// }